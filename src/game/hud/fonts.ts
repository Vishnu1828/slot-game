// Bitmap font families used by the HUD. These strings MUST match the internal `face` name inside
// each .fnt file in common/fonts (not the filename). If your Inter .fnt files declare a different
// face (e.g. "Inter"), update these to match — otherwise BitmapText won't find the font.
export const FONT_REGULAR = "Inter_Regular"; // labels ("Balance", "Total Bet")
export const FONT_BOLD = "Inter_Bold"; // numbers ("$100.000", "$5")
